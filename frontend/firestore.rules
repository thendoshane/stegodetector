rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DANGER: Allows anyone to read/write. Use only for development.
      allow read, write: if true;
    }
  }
}